#include<stdio.h>
#include<string.h>
#include<math.h>
#include<stdlib.h>

int main()
{
    int n,m,a,b,k;
    scanf("%d %d",&n,&m);
    long *ar,sum =0,answer = -1;
    ar = (long*)malloc(sizeof(long)*10000000);
    for(int i=0;i<m;i++)
    {
        scanf("%i %i %i",&a,&b,&k);
    }

    return 0;
}